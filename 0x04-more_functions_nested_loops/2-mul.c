#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply eith the first
 * Return: the result of multiplication betwen x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
